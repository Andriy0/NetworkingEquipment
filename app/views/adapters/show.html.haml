.text-center
  %h1.display-5.text-secondary.mb-5 More details

  %h4.mb-4= @adapter.name
  
.d-flex.justify-content-around
  .d-inline-block
    %p.h5= @adapter.vendor.name
    %p.h5= @adapter.adapter_type.name
    %p.h5= @adapter.connection_type.name
    
.text-center.mt-4
  = link_to 'Home', root_path, class: 'btn btn-outline-primary'
  = link_to 'Edit', edit_adapter_path(@adapter), class: 'btn btn-outline-secondary'
  = link_to 'Destroy', adapter_path(@adapter), class: 'btn btn-outline-danger',
    data: {method: :delete, confirm: "Are you sure?"}
